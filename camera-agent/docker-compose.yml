services:
  camera-agent:
    build:
      context: .
      dockerfile: Dockerfile
      network: host
    container_name: disguise-camera-agent-edge
    restart: unless-stopped
    env_file:
      - .env
    network_mode: "host"
