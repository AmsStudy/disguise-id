services:
  camera-agent:
    build:
      context: .
      dockerfile: Dockerfile
      network: host
    container_name: disguise-camera-agent-edge
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - .:/app
      - ~/.insightface:/root/.insightface
    network_mode: "host"
